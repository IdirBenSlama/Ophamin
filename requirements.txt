# Ophamin — runtime dependencies (mirrors pyproject.toml [project].dependencies)
#
# Install via:    pip install -r requirements.txt
# Or via project: pip install -e .
#
# The framework's pillars delegate to these mature libraries rather than
# rolling their own statistics. lxml + rdflib are required because prov's
# serializer registry eagerly imports every serializer (XML, RDF) even when
# only PROV-JSON is used. psutil powers the instrumenting wheel's per-cycle
# CPU / RSS / page-fault sampler.

numpy>=1.24
scipy>=1.10
pyyaml>=6.0
pandas>=2.0
scikit-learn>=1.3
statsmodels>=0.14
mapie>=1.0
river>=0.21
prov>=2.0
lxml>=4.9
rdflib>=6.0
omegaconf>=2.3
mlflow>=2.9
dvc>=3.0
psutil>=5.9
